# sagi = sudo apt-get install (to be added in ~/.bashrc_aliases)

# Adding current user to www-data group
sudo adduser $LOGNAME www-data

# Basics enhancements
sagi tree vim exuberant-ctags linux-headers-generic build-essential rar unrar zip unzip p7zip p7zip-full p7zip-rar manpages-fr manpages-fr-extra

# Shell enhancement
sagi htop fortunes-fr vrms linuxlogo sysstat di discus pydf hardinfo lynx ack-grep pandoc most

# Nautilus enhancement
sagi nautilus-actions nautilus-open-terminal nautilus-image-converter nautilus-open-terminal regexxer

# Usefull basic softwares
sagi filezilla gimp vlc ffmpeg gmountiso bleachbit fslint sound-juicer lame wine msttcorefonts gtk-recordmydesktop powertop acpi

# Dev GUI & tools
sagi tidy markdown git git-core git-gui git-doc git-svn git-email gitk tig
sagi meld virtualbox-ose virtualbox-ose-guest-utils cloc umbrello dia gaphor

# Web dev tools
sagi gurlchecker linkchecker google-sitemapgen skipfish

# Education
sagi tuxtype gtypist celestia planets stellarium gbrainy

# Services management
# (ignored) apt-get install sysv-rc-conf

# Office stuff
sagi freemind planner collatinus conduit grsync hamster

# Network
# (ignored) sagi nmapsi4 umit

# Dev
sagi apache2 apache2-mpm-prefork libapache2-mod-php5 apache2-utils php5 php5-dev phpmyadmin mysql-server
sagi imagemagick php-apc php5-xdebug php5-mysql php5-sqlite php5-cli php5-curl php5-suhosin php-pear php5-gd php5-imagick php5-imap php5-xsl php5-common php5-mcrypt php5-memcache php5-ps php5-pspell php5-recode php5-snmp php5-tidy php5-intl php-geshi

MongoDB :
cf. http://docs.mongodb.org/manual/tutorial/install-mongodb-on-ubuntu/

STOPPED HERE

TODO : Skype GoogleEarth GoogleChat

# PEAR, PhpUnit & dependencies, Xdebug
sudo pear channel-discover pear.symfony-project.com
sudo pear channel-discover pear.phpunit.de
sudo pear channel-discover pear.pdepend.org
sudo pear upgrade-all
sudo pear install phing/phing
sudo pear install phpunit/PHPUnit
sudo pear install PhpDocumentor
sudo pear install phpunit/phpcpd
sudo pear install pdepend/PHP_Depend-beta
sudo pear install PHP_CodeSniffer
sudo pecl install xdebug
sudo apt-get update && sudo apt-get upgrade

# conf de php5-xdebug :
sudo pecl install xdebug
find / -name 'xdebug.so' 2> /dev/null
+ 6 lignes à /etc/php5/apache2/php.ini :
; Xdebug
zend_extension="/usr/lib/php5/20090626/xdebug.so"
xdebug.remote_enable=On
xdebug.remote_host="localhost"
xdebug.remote_port=9000
xdebug.remote_handler="dbgp"


Installing xdebug : http://matthewturland.com/2010/01/03/phpunit-and-xdebug-on-ubuntu-karmic/
# Configure MySQL & Apache2 services as manual
http://doc.ubuntu-fr.org/lamp
# Configure xdebug + Eclipse + LAMP
http://doc.ubuntu-fr.org/eclipse_php_xdebug
# Configure php-apc

    # +2 lines in /etc/php5/apache2 & cli/php.ini

        apc.enabled = 1
        apc.enable_cli = 1

    # Then restart apache2

# VirtualHosts
http://doc.ubuntu-fr.org/tutoriel/virtualhosts_avec_apache2

# Music
sagi solfege audacity alsa-oss banshee banshee-extension-ubuntuonemusicstore songbird

# Son sous Dell 1555 :
sudo vim /etc/modprobe.d/alsa-base.conf
	=> Add :
		# Son pour Dell 1555
		options snd-hda-intel model=dell-m6


-------------- /usr/bin/lamp ou /bin/lamp

cf. http://www.galipe.net/articles/2011/02/
cf. http://castyour.net/dev-machine-lamp-stop-start-restart

#!/bin/sh

case "$1" in
  start)
#        /etc/init.d/apache2 start
#        /etc/init.d/mysql start
        service apache2 start
        service mysql start
        ;;
  stop)
#        /etc/init.d/apache2 stop
#        /etc/init.d/mysql stop
        service apache2 stop
        service mysql stop
        ;;
  reload)
#        /etc/init.d/apache2 reload
#        /etc/init.d/mysql reload
        service apache2 reload
        service mysql reload
        ;;
  restart)
#        /etc/init.d/apache2 restart
#        /etc/init.d/mysql restart
        service apache2 restart
        service mysql restart
        ;;
  *)
        echo "Usage: lamp {start|stop|restart|reload}"
        exit 1
        ;;
esac

exit 0

# LibreOffice:
sudo apt-get purge "openoffice*.*"
sudo add-apt-repository ppa:libreoffice/ppa
upup
sudo apt-get install libreoffice libreoffice-gnome language-support-fr
upup

# Open Ubuntu menu using Windows Key
gconftool-2 --set /apps/metacity/global_keybindings/panel_main_menu --type string "Super_L"

# markdown file icon in Ubuntu is a genesis ROM => change this : http://goo.gl/rzleM



